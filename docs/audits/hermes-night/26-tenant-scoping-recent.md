# Изоляция организаций в маршрутах и запросах, изменённых за последние 45 дней

Ветка `hermes/q3-0925` (worktree `D:\PillingR\wt-night`), окно анализа — коммиты за 45 дней до 26.09.2026.
Область: `src/app/api`, `src/modules`, `src/services` — в окне изменений 427 путей, из них 139 файлов маршрутов `route.ts`; на диске сейчас 123 из этих 139 (остальные удалены или переименованы), всего в `src/app/api` — 142 маршрута.

## Итог

- **критично — 0.** Ни одного воспроизводимого сегодня межорганизационного или межэкипажного чтения/записи не найдено. Операторский и помощниковый контуры закрыты плотнее всего: команды смены идут через `requireCrew`/`requireOpenShift` (`src/modules/operator-mobile/application/commands/shared.ts:30`, `:52`), моточасы и топливо — через `assertOperatorOwnsEquipment` (`src/app/api/equipment/[id]/meter-readings/route.ts:20`), осмотры — через `performedById` оператора (`src/app/api/inspections/[id]/route.ts:36`).
- **важно — 11.** Все — «фильтр отваливается, когда `tenantId` пуст» либо «фильтра нет вовсе на таблице с `tenantId`». Сегодня не выстреливают: у действующих пользователей `tenantId` проставлен, а роли `FOREMAN`/`SAFETY_ENGINEER` пользователей не имеют (доверительная модель AGENTS.md). Выстрелят при тенанте №2 или при пользователе без организации.
- **мелочь — 7.** Стиль и защита «второй линии».
- Топ-5 по значению:
  1. `src/core/infrastructure/raw-queries.ts:70` — пустой `tenantId` превращается в `tenantId IS NULL`: вместо отказа запрос возвращает строки без организации, то есть устаревшие данные всех организаций сразу.
  2. `src/modules/reports/application/queries/report-query.service.ts:58` и `:94` — при пустом `tenantId` фильтр молча не добавляется вовсе (не «пусто», а «всё»).
  3. `src/services/analytics/equipment-analytics-service.ts:156` — агрегат по `TelemetryRecord` без `tenantId` (таблица с `tenantId`).
  4. `src/app/api/media/[id]/route.ts:17` и `src/app/api/media/[id]/confirm/route.ts:17` — в выборке нет `tenantId`, поэтому тенантное равенство в `assertCanAccessMedia` (`src/core/media/media-auth.ts:184`, `:195`) не может быть истинным никогда: вторая линия обороны мертва (сейчас закрыто первой — владением/ролью).
  5. `src/core/security/tenant-enforcement.ts:71` и `src/services/tenancy/tenant-enforcement-middleware.ts:56` — целые модули с fail-open семантикой (`tenantWhere` без режима multi возвращает условие как есть, `requireTenant` бросает только в multi) остаются экспортированными и соблазнительными для нового кода.

Что закрыто правильно и почему это не находка: удаление отчёта (`src/app/api/reports/delete/route.ts:36` — `findFirst` с `tenantId` в `WHERE` до необратимого `delete`), ключи устройств (`src/app/api/equipment/[id]/device-keys/route.ts:51` — отказ при пустом `tenantId`, оборудование ищется парой `id+tenantId`), состояние помощника (`src/modules/operator-mobile/application/assistant-query.ts:71` — связь бригады проверяется через `equipment.tenantId`), история изменений (`src/services/audit/audit-history-service.ts:80` — `requireTenantId`).

## Методика

1. Окно изменений:

```bash
git log --since='45 days ago' --name-only --format= -- src/app/api src/modules src/services | sort -u
# 427 путей после sort -u; из них 139 — файлы маршрутов src/app/api/**/route.ts
```

2. Отсев несуществующих (часть путей в логе — удалённые файлы: 74 из 427) и сортировка оставшихся маршрутов по числу упоминаний `tenantId`, чтобы читать в первую очередь файлы без него:

```bash
while read -r f; do [ -f "$f" ] && echo "$f"; done < ch.txt > existing.txt   # 353 существующих
for f in $(grep '^src/app/api' existing.txt); do echo "$(grep -c tenantId "$f") $f"; done | sort -n
```

Читались и сами удалённые пути: если маршрут убрали, проверять в нём нечего (так отпал весь `src/modules/operator-v3/**`).

3. Точечные grep'ы по признакам нарушения (везде — с контекстом, чтобы видеть проверку целиком):

```bash
rg -n 'where: \{ id' src/app/api                      # поиск по первичному ключу
rg -n 'tenantId\s*(\?\?|\|\|)\s*(undefined|null)' src  # отваливающийся фильтр
rg -n 'DEFAULT_TENANT_ID|x-tenant-id' src              # подстановка организации
rg -n 'findUnique|findFirst|groupBy|\$queryRaw' <файл> # точка доступа к данным + есть ли tenantId рядом
```

4. Примитивы изоляции прочитаны целиком, чтобы понимать семантику вызовов: `src/lib/tenant.ts`, `src/lib/tenant-scope.ts`, `src/core/security/tenant-enforcement.ts`, `src/services/tenancy/tenant-context-service.ts`, `src/services/auth/authorization-service.ts` (матрица прав: `reports.read_all` — ADMIN/DISPATCHER/FOREMAN/SAFETY_ENGINEER, `isPrivilegedRole` — только ADMIN/DISPATCHER).

5. Для каждой находки проверялся путь достижения: маршрут → право (`assertCan`/`requireAuth`) → сервис. Severity ставилась по достижимости сегодня, с учётом доверительной модели AGENTS.md (ADMIN/DISPATCHER межорганизационны по замыслу).

Что не проверялось инструментально: сервер не запускался, ни один запрос не отправлялся (по условию задачи), RLS-политики в БД не инспектировались — выводы сделаны только по коду.

## Находки

| # | severity | path:line | проблема | сценарий / почему важно | как чинить |
|---|---|---|---|---|---|
| 1 | важно | `src/core/infrastructure/raw-queries.ts:70` | `where.tenantId = tenantId && tenantId.trim().length > 0 ? tenantId : null` — пустой `tenantId` не отклоняется, а превращается в `tenantId IS NULL` | Вызывающий `getReportsByPeriod(dateFrom, dateTo, siteId, tenantId, userId)` приходит из `src/app/api/reports/period/route.ts:52` с `user?.tenantId \|\| null`. Пользователь без организации (или сбой чтения строки пользователя) вместо 403 получает строки отчётов без организации — то есть историческое наследие всех организаций сразу. Это ровно тот шаблон `IS NULL OR ...`, который запрещён правилом проекта | `if (!tenantId \|\| !tenantId.trim()) throw new ServiceError('tenantId is required', 400);` до построения `where` (образец — `src/modules/equipment/application/queries/equipment-query.service.ts:338`) |
| 2 | важно | `src/modules/reports/application/queries/report-query.service.ts:58` (`getEditableReport`) | `if (role !== 'ADMIN' && role !== 'DISPATCHER' && sessionUser.tenantId) where.tenantId = ...` — при пустом `tenantId` условие не добавляется вообще | Достижимо через `GET /api/reports/edit` (право не требуется, только вход). Роль `FOREMAN`/`SAFETY_ENGINEER` и пользователь без организации → выборка `findFirst` без тенантного фильтра: чужой отчёт отдаётся по `userId+siteId+date`. Сегодня сдержано только отсутствием таких пользователей | Требовать `requireTenantId(sessionUser)` в начале функции и добавлять фильтр безусловно для не-привилегированных; привилегированность считать через `isPrivilegedRole` |
| 3 | важно | `src/modules/reports/application/queries/report-query.service.ts:94` (`listReportsForReview`) | та же условная вставка фильтра, плюс `total = db.report.count({ where })` считается по тому же условию (`:109`) | Достижимо через `GET /api/reports/all` (`src/app/api/reports/all/route.ts:27`, право `reports.read_all`). Фильтр по `tenantId` отваливается вместе с организацией пользователя, а не отказывает: список и итог показывают свод по всем организациям | То же: `requireTenantId` + безусловный фильтр для не-привилегированных |
| 4 | важно | `src/app/api/media/[id]/route.ts:17` и `src/app/api/media/[id]/confirm/route.ts:17` | `select` не содержит `tenantId`, а `assertCanAccessMedia` проверяет `media.tenantId === actor.tenantId` (`src/core/media/media-auth.ts:184` для техники, `:195` для отчётов/осмотров/ТО) | `undefined === tenantId` ложно всегда, поэтому тенантная ветка не срабатывает никогда. Сейчас утечки нет: остаётся проверка владения (`:194`) и роли (`:193`), а для техники — отказ по `!media.tenantId` (`:184`). Но вторая линия обороны мертва, и любой будущий перенос проверки тенанта на неё даст дыру молча. Побочный эффект уже есть: `FOREMAN`/`SAFETY_ENGINEER` не проходят ветку `:195-198` и получают 403 на чужие фото отчётов | Добавить `tenantId: true` в `select` в обоих маршрутах (как сделано в `src/app/api/media/[id]/download/route.ts:22`) |
| 5 | важно | `src/services/analytics/equipment-analytics-service.ts:156` | `db.telemetryRecord.groupBy({ by: ['equipmentId'], where: { type, timestamp, ...(siteId ? { siteId } : {}) } })` — ни `tenantId`, ни `siteId` обязательны | Таблица `TelemetryRecord` тенантная (рядом, на `:142`, тот же сервис фильтрует `r."tenantId" = ${tenantId}`), но здесь фильтра нет вовсе. Ответ сегодня не утекает: `fuelByEquipment` (`:166`) применяется к строкам, отобранным тенантным запросом (`:130`), по совпадению `equipmentId` — то есть в ответ попадают только id своей организации. Однако агрегат читает строки всех организаций (нагрузка) и при первом же соединении по чужому `equipmentId` вернёт чужой расход | Добавить `tenantId` в `where` (`siteId` оставить опциональным) |
| 6 | важно | `src/services/reports/event-handlers.ts:72` | проекция `ReportAnalytics` пишется с `tenantId: tenantId \|\| null` (fallback события на строку отчёта — `:51-57`) | Событие без `tenantId` и отчёт без `tenantId` (наследие, заведённое до включения тенанта) дают строку анализа без организации. Такая строка невидима для всех тенантных запросов (сломанная аналитика), а для запроса с `tenantId IS NULL` (находка №1) — видима всем | Падать или писать `tenantId` строки отчёта строго; при отсутствии — не создавать проекцию и логировать (как сделано для `siteId`/`userId` на `:59`) |
| 7 | важно | `src/modules/settings/application/settings-service.ts:44` | `const resolved = tenantId \|\| process.env.DEFAULT_TENANT_ID;` и далее `if (!resolved) return true` (`:45`) — то есть при отсутствии организации функции отвечает «уведомления включены» | Подстановка организации по умолчанию из окружения — тот самый шаблон, против которого написан `src/lib/tenant.ts`. Для организации без настроек это отвечает чужой конфигурацией; при отсутствии переменной — «включено» вместо отказа | `requireTenantId(tenantId)` и `false` (или исключение) при отсутствии настройки |
| 8 | важно | `src/core/security/tenant-enforcement.ts:71`, `:89`, `:168`, `:185` | `resolveTenantId` = `headerTenantId \|\| user.tenantId \|\| process.env.DEFAULT_TENANT_ID` (`:73`); `requireTenant` бросает только при `isMultiTenantMode()` (`:92`); `tenantWhere` без multi возвращает условие как есть (`:168`); `assertTenantOwnership` без multi — no-op (`:186`) | Модуль экспортируется (`src/core/security/index.ts`, `src/core/application/index.ts`) и выглядит как штатный инструмент изоляции. Любой новый маршрут, взявший отсюда `tenantWhere`, в single-tenant режиме получит выборку без фильтра, а `assertTenantOwnership` не будет проверять ничего — то есть защита, которая не защищает | Пометить модуль `@deprecated` и запретить импорт (правило в `no-restricted-imports`), либо убрать fail-open ветку: бросать при пустом `tenantId` независимо от режима |
| 9 | важно | `src/services/tenancy/tenant-enforcement-middleware.ts:56`, `:68` | `requireTenant()` бросает только в multi (`:58`), `tenantWhere(where)` в single возвращает условие без фильтра (`:75`) | Вторая копия той же fail-open пары, тоже экспортированная. Два разных модуля с одинаковым именем `tenantWhere` и разной семантикой — гарантированный источник ошибки при копировании | То же: либо удалить, либо сделать fail-closed; оставить один модуль на проект |
| 10 | важно | `src/services/tenancy/tenant-context-service.ts:31` | `const tenantId = request?.headers.get('x-tenant-id') \|\| defaultTenantId;` — организация берётся из заголовка запроса, а не из сессии | Заголовок подконтролен клиенту. В multi-режиме это позволило бы запросить чужую организацию, если `resolveTenantContext` где-то дойдёт до данных; сегодня — только контекст корреляции, но приоритет заголовка над сессией противоречит модели «организация только из сессии» (ср. комментарий `src/app/api/equipment/[id]/device-keys/route.ts:49`) | Источник — сессия; заголовок использовать только для логирования и только после сверки с сессией |
| 11 | важно | `src/modules/reports/application/commands/report-command.service.ts:136-142` | тенантное равенство применяется только если `existingState.tenantId != null && input.tenantId != null`; при пустом тенанте в старой записи проверка пропускается | Осознанное решение (комментарий `:133-135`), но фактически это «строгое равенство, кроме случая пусто» — то есть ровно тот шаблон, который правило проекта запрещает в запросах. Спасает только проверка владельца на `:130`. Пока она есть — не дыра; при её ослаблении («админ правит чужой отчёт») тенантная линия уже не сработает | Мигрировать наследие (заполнить `tenantId` у старых отчётов) и оставить только строгое равенство |
| 12 | мелочь | `src/app/api/reports/upsert/route.ts:74` | `tenantId: tenantId \|\| undefined` при уже вызванном `requireTenantId(user!)` (`:66`) | Сейчас безвредно (пустая строка не доходит), но это образец для копирования: следующий автор решит, что «пусто = undefined» — допустимое состояние | Передавать `tenantId` как есть |
| 13 | мелочь | `src/app/api/crews/all/route.ts:24-27` + `src/lib/cached-queries.ts:36-51` | `getCachedCrewsAll()` тянет все бригады всех организаций под ключом кэша `crews:all` (без организации), а фильтр `crew.site?.tenantId === tenantId` делается в JavaScript после выборки | Ответ сегодня верный, но: (а) полный межорганизационный набор лежит в кэше и переиспользуется всеми; (б) бригады без объекта (`site` = null) отсеиваются как чужие; (в) если фильтр когда-нибудь уберут при рефакторинге, утечка будет мгновенной и незаметной | Ключ кэша с организацией (`crews:${tenantId}:all`) и фильтр в SQL |
| 14 | мелочь | `src/app/api/equipment/route.ts:22` | `const operatorUserId = user!.role === 'OPERATOR' ? user!.id : null;` — помощник (`ASSISTANT`) не сужается ни по бригаде, ни по объекту; права на маршруте нет вовсе (только вход) | Внутри одной организации помощник видит весь парк. Для сравнения, `src/app/api/monitoring/fleet/route.ts` сужает и помощника. Расхождение двух маршрутов по одному и тому же вопросу — источник будущей ошибки | Решить, положен ли помощнику весь парк; если нет — сузить тем же приёмом, что в мониторинге |
| 15 | мелочь | `src/app/api/crews/route.ts:29` | `getAccessibleCrews(tenantId, siteId \|\| undefined, pagination)` — `siteId` из строки запроса не проверяется на доступность актору | Тенантный фильтр держит, поэтому утечки нет; но фильтр по чужому объекту своей организации молча даёт пустой список вместо отказа — то же семейство, что `assertSiteAccess` в других маршрутах | Проверять доступ к объекту перед выборкой |
| 16 | мелочь | `src/modules/crews/application/queries/crew-query.service.ts:54` (`getCrewForOperator`) + `src/app/api/crews/my/route.ts:21` | сама выборка бригады тенант-blind (фильтр только по `operatorId`), безопасность целиком на вызывающих | В `crews/my` вторая проверка есть (`ensureTenantAccess(user!, crew.tenantId, ...)` на `:24`) — поэтому не дыра. Но в `src/app/api/equipment/[id]/fuel/route.ts:23` и `meter-readings/route.ts:25` результат сравнивается с `equipmentId` и тенант не сверяется вовсе: равенство id-шников там и есть проверка. Ослабление одного вызывающего не заметит никто | Добавить `tenantId` в сигнатуру `getCrewForOperator` |
| 17 | мелочь | `src/services/reports/event-handlers.ts:111-142` | `recomputeSiteDailySummary(siteId, date)` читает отчёты и пишет `SiteDailySummary` (`:138-141`) без `tenantId`; `db.report.findMany({ where: { siteId, date, status } })` (`:115`) | Сегодня `siteId` приходит из проверенных маршрутов (`src/app/api/reports/delete/route.ts:68` — объект уже сверен с организацией), поэтому утечки нет. Но агрегат по объекту без организации и строка проекции без тенанта сломаются, если объекты когда-нибудь перестанут быть глобально уникальными | Передавать `tenantId` в функцию и в `where`/`upsert` |
| 18 | мелочь | `src/app/api/reports/all/route.ts:27` + `src/services/auth/authorization-service.ts:131` | право проверяется по исполняемой роли (`can` учитывает `actingAs`, `:151-153`), а тенантный фильтр в сервисе — по собственной (`sessionUser.role`) | Администратор, работающий «как мастер» (`actingAs`), проходит `assertCan` по правам мастера, но фильтр по организации у него отключается как у ADMIN. По доверительной модели ADMIN межорганизационен по замыслу, поэтому это не утечка, но расхождение двух трактовок роли в одном решении надо зафиксировать | Использовать `isPrivilegedRole(resolveEffectiveRole(...))` в обоих местах |

## Не проверено

- `src/core/notifications/telegram.ts` — **вызов подтверждён** (`src/app/api/alerts/webhook/route.ts:14` импортирует `telegramNotifier`, `:113` вызывает `sendAlert`; сам маршрут открыт без входа и защищён только `?token=` против `ALERTMANAGER_WEBHOOK_TOKEN`, `:8`), а вот тело модуля в этой сессии не открывалось (вывод чтения пришёл без содержимого), поэтому подстановка организации по умолчанию там **не подтверждена**. Нужно открыть файл и проверить, берётся ли организация получателя из сессии или из `DEFAULT_TENANT_ID`.
- RLS-политики (`prisma/migrations/**`) не инспектировались: неизвестно, закрыт ли фильтром на уровне БД случай пустого `app.current_tenant`. Все выводы — по коду приложения. Если RLS закрывает, находки №1–№3 всё равно остаются актуальными как «следующий маршрут скопирует шаблон».
- `src/app/api/media/download-batch/route.ts`, `src/app/api/media/route.ts`, `src/app/api/feedback/events/route.ts`, `src/app/api/admin/analytics/overview/route.ts`, `src/app/api/orion/lead/route.ts` — читались как файлы, но их тела в этой сессии не сохранились в контексте; утверждать по ним нечего. По `media-auth.ts:136-149` (`filterReadableMedia` использует тот же `assertCanAccessMedia`, что и находка №4) есть косвенный признак той же проблемы с отсутствующим `tenantId` в выборках — требует отдельной проверки.
- `prisma/schema.prisma` проверен точечно: `DeadLetterQueue` (`:2815-2830`) поля `tenantId` не имеет вовсе, поэтому `src/app/api/admin/dlq/route.ts:37` не тенантная находка (маршрут закрыт правом `dlq.manage`).
- Замеры времени/нагрузки (число строк, читаемых без тенантного фильтра) не делались: БД не поднималась.
- Маршруты замороженных зон (`src/app/operator/**`, `src/app/(app)/operator/**`, `src/components/orion/**`, `src/app/api/orion/**`) просматривались только на предмет «есть ли тенантный доступ», без оценки логики; `src/modules/operator-v3/**` в окне изменений присутствует, но на диске уже удалён — проверять нечего.
