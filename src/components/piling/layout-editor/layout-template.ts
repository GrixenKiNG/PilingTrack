/**
 * Ре-экспорт доменных типов раскладки.
 *
 * Сам код живёт в modules/layout/domain — это чистые типы и валидаторы без
 * React, то есть домен, а не UI. Здесь остаётся тонкая обёртка, чтобы
 * компоненты продолжали импортировать привычный путь: modules/ не имеет права
 * зависеть от components/ (проверка границ слоёв), а обратное направление —
 * норма.
 */
export * from '@/modules/layout/domain/layout-template';
