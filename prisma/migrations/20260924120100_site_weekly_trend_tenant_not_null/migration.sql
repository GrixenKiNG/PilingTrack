-- NOT NULL уже стоит на проде и локально: его поставил ручной скрипт
-- scripts/audit-fix-tenancy.sql мимо миграций, а schema.prisma осталась с
-- необязательным полем — расхождение всплывало в каждом `migrate diff`.
-- Здесь ограничение закрепляется в истории, чтобы новая база получала его
-- тоже. На живых базах — без изменений (строк с пустым tenantId: 0,
-- проверено 24.09.2026).
ALTER TABLE "SiteWeeklyTrend" ALTER COLUMN "tenantId" SET NOT NULL;
