-- Погружение добойником: сваю добивают инструментом, чтобы голова ушла ниже
-- уровня грунта. Только в этом случае глубина погружения может превысить длину
-- самой сваи — в остальных это опечатка, и правило её отклоняет.
ALTER TABLE "PilePassport" ADD COLUMN "followerUsed" BOOLEAN NOT NULL DEFAULT false;
