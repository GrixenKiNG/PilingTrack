-- Refresh-токены удалены из кода 24.09.2026: createRefreshToken нигде не
-- вызывался, POST /api/auth/refresh не мог сработать ни разу, таблица на
-- проде пуста (0 строк, входящих внешних ключей нет — проверено перед
-- миграцией). Политики RLS, индексы и собственные ограничения уходят вместе
-- с таблицей.
DROP TABLE IF EXISTS "RefreshToken";
