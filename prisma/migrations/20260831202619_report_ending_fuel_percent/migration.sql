-- Остаток топлива на конец смены, в процентах по указателю.
-- Снимается оператором в ЕО после работы и показывается следующей смене при
-- приёме установки. Столбец необязательный: у смен, закрытых до этой правки,
-- значения нет и взять его неоткуда.
ALTER TABLE "Report" ADD COLUMN "endingFuelPercent" INTEGER;
